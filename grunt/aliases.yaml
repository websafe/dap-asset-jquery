setup: []

default:
  description: 'Default'
  tasks:
    - 'clean:all'
    - 'copy:plugin_src'
    - 'copy:license'
    - 'copy:jquery'
    - 'copy:jquery_legacy'

distclean:
  description: 'Remove built and intermediate files and node|bower_components also.'
  tasks:
    - 'clean:distclean'

build:
  description: 'Build'
  tasks:
    - 'clean:all'
    - 'copy:plugin_src'
    - 'copy:license'
    - 'copy:jquery'
    - 'copy:jquery_legacy'
